# R
.Rhistory
.RData
.Rproj.user/
*.Rproj

# renv
renv/library/
renv/staging/

# rsconnect deployment metadata (created by deploy.R / shinyapps.io)
rsconnect/

# Local R environment (may contain private paths or tokens)
.Renviron

# Quarto build artefacts
/.quarto/
_freeze/
_site/

# Data files — kept local, not committed to git.
# For GitHub Pages: data is embedded in docs/index.html at render time (ojs_define).
# For shinyapps.io: run deploy.R locally; it bundles data directly from disk.
data/*.txt
data/*.tsv
data/*.csv

# OS
.DS_Store
Thumbs.db

# Editor / AI assistant session data
.continue/
.claude/

# Runtime logs
*.log
